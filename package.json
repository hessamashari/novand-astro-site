{
  "name": "novand-site",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0 --port 3000",
    "build": "node scripts/generate-og-image.mjs && node scripts/export-brand-assets.mjs && astro build",
    "preview": "astro preview --host 0.0.0.0 --port 3000",
    "lint": "astro sync && astro build"
  },
  "dependencies": {
    "@astrojs/sitemap": "latest",
    "@formspree/ajax": "^1.1.5",
    "@tailwindcss/vite": "latest",
    "astro": "latest",
    "tailwindcss": "latest",
    "zod": "latest"
  }
}
